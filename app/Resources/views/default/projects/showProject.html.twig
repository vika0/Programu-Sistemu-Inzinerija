
{% extends "::base.html.twig" %}

{% block body %}
        <div class="container">
            <div class="row">
                <div class="text-center">
                    <h1>{{ project.title }}</h1>
                </div>
                <p>{{ project.description }}</p>
            </div>
        </div>

{% endblock %}